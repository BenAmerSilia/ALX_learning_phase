#include <stdio.h>

int main() {
	    float length, width;

	        printf("Enter the length ,then the width of your rectangular:\n ");
		    scanf("%f %f", &length, &width);
			    printf("The area of your rectangle is: %.2f\n", width*length);

			        return 0;
}

